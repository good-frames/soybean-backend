<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soybean.upms.mapper.SysUserRoleMapper">

    <resultMap type="com.soybean.upms.api.po.SysUserRole" id="SysUserRoleResult">
        <result property="userId"    column="user_id"    />
        <result property="roleId"    column="role_id"    />
    </resultMap>

    <select id="countUserRoleByRoleId" parameterType="Long" resultType="Integer">
        select count(1) from sys_user_role where role_id = #{roleId}
    </select>

    <delete id="deleteUserRoleByUserId" parameterType="Long">
        delete from sys_user_role where user_id = #{userId}
    </delete>

    <delete id="deleteUserRoleByUserIds" parameterType="String">
        delete from sys_user_role where user_id in
        <foreach item="userId" collection="userIds" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <delete id="deleteUserRoleByRoleId" parameterType="Long">
        delete from sys_user_role where role_id = #{roleId}
    </delete>

    <insert id="batchUserRole">
        insert into sys_user_role(user_id, role_id) values
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId})
        </foreach>
    </insert>

</mapper>
