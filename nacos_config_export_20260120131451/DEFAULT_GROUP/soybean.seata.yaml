seata:
    registry:
        # support: nacos, eureka, redis, zk, consul, etcd3, sofa
        type: nacos
        nacos: 
            application: seata-server
            server-addr: nacos_ip:nacos_port
            group: DEFAULT_GROUP
            namespace: soybean-dev
            # tc集群名称
            cluster: default
            username: nacos
            password: nacos
    server:
        service-port: 8091